name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  backend:
    name: Backend Tests
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: mordors_edge_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U postgres"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    env:
      RAILS_ENV: test
      DATABASE_HOST: localhost
      DATABASE_PORT: 5432
      DATABASE_USER: postgres
      DATABASE_PASSWORD: postgres

    defaults:
      run:
        working-directory: backend

    steps:
      - uses: actions/checkout@v4

      - name: Install system dependencies
        working-directory: .
        run: |
          sudo apt-get update -qq
          sudo apt-get install --no-install-recommends -y libpq-dev

      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: "4.0"
          working-directory: backend

      - name: Install dependencies
        id: bundle_install
        continue-on-error: true
        run: bundle install --jobs 4 --retry 3 2>&1 | tee /tmp/bundle-output.txt

      - name: Upload bundle output on failure
        if: steps.bundle_install.outcome == 'failure'
        uses: actions/upload-artifact@v4
        with:
          name: bundle-install-output
          path: /tmp/bundle-output.txt

      - name: Fail if bundle install failed
        if: steps.bundle_install.outcome == 'failure'
        run: |
          cat /tmp/bundle-output.txt
          exit 1

      - name: Set up database
        id: db_setup
        continue-on-error: true
        run: bundle exec rails db:prepare 2>&1 | tee /tmp/db-output.txt

      - name: Upload db output on failure
        if: steps.db_setup.outcome == 'failure'
        uses: actions/upload-artifact@v4
        with:
          name: db-setup-output
          path: /tmp/db-output.txt

      - name: Fail if db setup failed
        if: steps.db_setup.outcome == 'failure'
        run: |
          cat /tmp/db-output.txt
          exit 1

      - name: Run RSpec
        id: rspec
        continue-on-error: true
        run: bundle exec rspec 2>&1 | tee /tmp/rspec-output.txt

      - name: Upload RSpec output on failure
        if: steps.rspec.outcome == 'failure'
        uses: actions/upload-artifact@v4
        with:
          name: rspec-output
          path: /tmp/rspec-output.txt

      - name: Fail if RSpec failed
        if: steps.rspec.outcome == 'failure'
        run: |
          cat /tmp/rspec-output.txt
          exit 1

  frontend:
    name: Frontend Checks
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: frontend

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Lint (Biome)
        run: npx biome check .

      - name: TypeScript check
        run: npx tsc -b --noEmit

      - name: Run tests (Vitest)
        run: npm run test

      - name: Build
        run: npm run build
